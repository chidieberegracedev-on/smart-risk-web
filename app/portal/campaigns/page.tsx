import { PhaseStub } from "@/components/portal/phase-stub";

export default function Page() {
  return (
    <PhaseStub
      title="Campaigns"
      body="The full campaign table — filters, search, and per-campaign detail — is being built next. Your campaigns are safe and visible in the app meanwhile."
    />
  );
}
