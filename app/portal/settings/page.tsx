import { PhaseStub } from "@/components/portal/phase-stub";

export default function Page() {
  return (
    <PhaseStub
      title="Settings"
      body="Profile, theme, and currency preferences are coming here. The theme toggle in the top bar already works and is remembered."
    />
  );
}
