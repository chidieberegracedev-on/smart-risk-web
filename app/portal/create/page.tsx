import { PhaseStub } from "@/components/portal/phase-stub";

export default function Page() {
  return (
    <PhaseStub
      title="Create Promotion"
      body="The web promotion builder — boost a post or create a new ad, with live audience and price quotes — arrives in the next update. You can create promotions in the app today."
    />
  );
}
